# Write your solution here

d = {}
while True:
  command = input("command (1 search, 2 add, 3 quit): ")
  if command == "2":
    name = input("name: ")
    number = input("number: ")
    d[name] = number
    print("ok!")
  elif command == "1":
    search_name = input("name: ")
    if search_name in d:
      print(d[search_name])
    else:
      print("no number")
  elif command == "3":
    print("quitting...")
    break 
