# Write your solution here
from random import *
def words(n: int, beginning: str):
  final_words = []
  with open("words.txt") as file:
    for line in file:
      line = line.strip()
      if line.startswith(beginning):
        final_words.append(line)

  return sample(final_words, n)

