
def same_chars(char, a,b):

    if a < len(char) and b < len(char):
        if char[a] != char[b] or a > len(char) or b > len(char):
            return False
        else:
            return True
    else:
        return False

if __name__ == "__main__":
    print(same_chars("coder", 1, 10))
