# write your solution here
def read_fruits():
  final = {}
  with open("fruits.csv","r") as file:
    for line in file:
      line = line.replace("\n","")
      parts = line.split(";")
      final[parts[0]] = float(parts[1])
  
  return final
