def read_input(message:str, start:int, end:int):
  while True:
    try:
      input_str = input(message)
      number = int(input_str)
      if start < number < end:
        return number
    except ValueError:
      pass
    print(f"You must type in an integer between {start} and {end}")

