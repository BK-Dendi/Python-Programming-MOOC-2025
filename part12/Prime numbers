# Write your solution here
def prime_numbers():
    n = 2
    while True:
        # Check if n is a prime
        for i in range(2, n):
            if n % i == 0:
                break
        else:
            yield n  # yield n if it's prime
        n += 1
